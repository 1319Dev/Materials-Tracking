# Supabase → Project Settings → API
# The anon (publishable) key is safe to ship in the static site.
# Row Level Security limits each user to their own rows. Never use the service_role key here.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
